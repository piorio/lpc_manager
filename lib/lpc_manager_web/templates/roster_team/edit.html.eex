<h1>Edit Roster team</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.roster_team_path(@conn, :update, @roster_team)) %>

<span><%= link "Back", to: Routes.roster_team_path(@conn, :index) %></span>
